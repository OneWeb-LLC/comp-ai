x-logging: &default-logging
  driver: json-file
  options:
    max-size: '10m'
    max-file: '5'
    compress: 'true'

services:
  migrator:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    env_file:
      - packages/db/.env
<<<<<<< HEAD
    # Supabase pooler (:6543) rejects DDL; migrate deploy needs the direct URL.
    command: sh -lc "DATABASE_URL=\"${DIRECT_URL:-$DATABASE_URL}\" npx prisma migrate deploy --schema=packages/db/dist/schema.prisma"
=======
      - .env
    # Do not override DATABASE_URL here — empty ${DIRECT_URL} at compose-parse time
    # wipes env_file values. The migrator entrypoint resolves DIRECT_URL at runtime.
>>>>>>> adbaa31f1 (fix(docker): resolve DIRECT_URL at runtime, not compose parse)
    logging: *default-logging
  seeder:
    build:
      context: .
      dockerfile: Dockerfile
      target: migrator
    env_file:
      - packages/db/.env
<<<<<<< HEAD
    command: sh -lc "DATABASE_URL=\"${DIRECT_URL:-$DATABASE_URL}\" npx prisma generate --schema=packages/db/dist/schema.prisma && npx tsx packages/db/prisma/seed/seed.ts"
=======
      - .env
    command: sh -lc "export DATABASE_URL=\"${DIRECT_URL:-$DATABASE_URL}\"; npx prisma generate --schema=packages/db/dist/schema.prisma && npx tsx packages/db/prisma/seed/seed.ts"
>>>>>>> adbaa31f1 (fix(docker): resolve DIRECT_URL at runtime, not compose parse)
    logging: *default-logging
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
      args:
        NEXT_PUBLIC_BETTER_AUTH_URL: ${BETTER_AUTH_URL}
    ports:
      - '3000:3000'
    env_file:
      - apps/app/.env
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'curl -f http://localhost:3000/api/health || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
    command: sh -lc "node apps/app/server.js"
    logging: *default-logging
  portal:
    build:
      context: .
      dockerfile: Dockerfile
      target: portal
      args:
        NEXT_PUBLIC_BETTER_AUTH_URL: ${BETTER_AUTH_URL_PORTAL}
    depends_on:
      app:
        condition: service_started
    ports:
      - '3002:3000'
    env_file:
      - apps/portal/.env
    restart: unless-stopped
    healthcheck:
      test: ['CMD-SHELL', 'curl -f http://localhost:3000/ || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
    logging: *default-logging
